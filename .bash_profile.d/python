# -*- mode: sh; -*-
command -v python >/dev/null 2>&1 || return

# Add user pipenv venv to path
PATH=$HOME/.pipenv/.venv/bin:$PATH
export PATH

# Add python startup file
export PYTHONSTARTUP=$HOME/.python-startup.py
